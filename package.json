{
  "name": "mini-clicker-next",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/init-data.js && next dev",
    "build": "node scripts/init-data.js && next build",
    "start": "node scripts/init-data.js && next start",
    "lint": "next lint",
    "test": "node scripts/test-deploy.js",
    "init-data": "node scripts/init-data.js",
    "check-deploy": "node scripts/check-deployment.js",
    "deploy": "npx vercel",
    "deploy:prod": "npx vercel --prod"
  },
  "dependencies": {
    "next": "16.0.1",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "styled-components": "^6.1.19"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "^14.2.33",
    "next-pwa": "^5.6.0",
    "typescript": "^5",
    "vercel": "^48.1.6"
  }
}
